"""페이지 레이아웃 분석: 꼬리말 식별, 제목 분류, 문단 시작 감지."""
from __future__ import annotations

import logging

logger = logging.getLogger(__name__)

# 캘리브레이션 근거 (2026-07-07, scripts/dump_coords.py로 페이지 2/10/28 실측):
#   꼬리말 yc: p10=0.067, p2=0.068, p28=0.065 (최대 0.068)
#   본문 최저 yc: p10=0.118, p2=0.128, p28=0.112 (최소 0.112)
#   꼬리말 w: p10=0.225, p2=0.219, p28=0.217 (최대 0.225)
#   본문 최대 w: p10=0.881, p2=0.856, p28=0.852 (평균 0.86)
#   들여쓰기 x 차이: p10 (0.126-0.104=0.022, 0.129-0.106=0.023), 평균 차이 0.025
#   제목 h / 본문 h: p2 0.035 / 0.023 ≈ 1.52
FOOTER_BAND: float = 0.09              # 꼬리말 yc 최대(0.068) + 본문 최저 yc(0.112) 중간값
FOOTER_MAX_WIDTH_RATIO: float = 0.30   # 꼬리말 w(0.225) / 본문 최대 w(0.86) ≈ 0.26 + 오탈자 여유
INDENT_MIN: float = 0.014              # 들여쓰기 차이(0.025) 절반, 안전 마진 적용
TITLE_HEIGHT_RATIO: float = 1.40       # 제목 h/본문 h 비율(1.52)와 1.0 사이 중간값

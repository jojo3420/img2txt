# PR Review Report: #4 — feat: address PR #3 review followups

> Generated: 2026-07-12 10:05 KST
> PR URL: https://github.com/jojo3420/img2txt/pull/4
> Base ← Head: main ← feat/pr3-review-followups
> Reviewer: deep-reasoner (Opus, 적대적 리뷰) + code-reviewer (Sonnet) 사전 검토

## 0. PR 요약

- 변경 규모: +282 / -65, 5개 파일
- 핵심: PR #3 Claude 리뷰 후속 3건 (보정본 stale 신호, 누락 표식 보정 제외, 트랜잭션-상태 리팩터)
- 로컬 검증: `pytest -m 'not macos'` 89 passed(기존 86 + 신규 3), `mypy --strict` 통과

## 1. 변경 항목

| 항목 | 리뷰 ID | 파일 |
|------|---------|------|
| 재시도 후 낡은 보정본 stale 신호 (`Job.correctedStale`) | H1 | server/models.py, server/pipeline.py |
| 누락 페이지 표식 LLM 보정 제외 + 원위치 보존 | M1 | img2txt/assembler.py, server/pipeline.py |
| `_replace_text_outputs` 헬퍼 분리 + 재시도 상태 판정 중복 제거 | M3/M5 | server/pipeline.py, server/jobs.py |

## 2. 적대적 리뷰 판정 (Opus)

**결론: MERGE-OK — Must-fix 0건.**

- 원자성 회귀: `git show origin/main:server/pipeline.py` 원본과 라인 단위 대조. 마커 쓰기→os.replace→marker.unlink→fsync→백업정리 순서와 실패 경로(커밋 fsync 실패 시 백업 유지, `_commit_replacements` 실패 시 복구+raise, marker.unlink 실패 시 복구+raise) 모두 원본과 동일. 회귀 없음.
- 데이터 손실(M1): `correct_paragraphs`가 입력 개수만큼 반환함을 corrector.py에서 확인 → `len(corrected_subset) == len(correctable_indices)` 보장, `zip` truncation 불가. 마커 원위치 보존 확인.
- stale 신호(H1): 오탐(최신인데 True)-미탐(낡았는데 False) 경로 없음. `book_corrected.txt`는 `run_correct_pipeline`만 생성하며 성공 시 False, 재시도 성공 시 True, 전면 보정 실패 시 미생성-기본 False.

## 3. 스코프에서 제외 (근거)

- **M2(손상 마커)**: fail-closed 유지가 정합성상 더 안전 — Codex 판정 존중.
- **M4(`running_count`)-M6(deep copy/TTL)**: 확장 여지-로컬 범위 밖.
- **[고아 백업 on marker 쓰기 실패]-[run_correct/retry_page 함수 길이]**: origin/main 이전부터 존재한 기존 이슈. "순수 리팩터" 스코프 밖 → 별도 이슈 권장.

## 4. Nice-to-have (비차단)

- `all_requests_failed` 경로에서 `correctedStale=False` 유지를 검증하는 명시적 테스트(현재 코드 분석으로만 확인).

## 5. 머지 결정

Must-fix 0건 → 사용자 승인(적대적 리뷰 + 스쿼시 머지)에 따라 SQUASH 머지 진행.
미검증 ⚠️: 실제 macOS Vision OCR 통합 경로(`macos` 마커)는 미실행.

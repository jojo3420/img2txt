### 종합 평가
- 전체 점수: 6.5/10
- 한줄 총평: 구현은 단순하고 기존 `txt` 경로도 보존됐지만, 라벨 검증과 A/B 판정 근거가 아직 약합니다.
- 머지 권고: 🟡 Should-fix 수정 후 머지

### 관점별 리뷰 (표)

| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | 🟡 보통 | 구현은 플랜과 일치하지만 판정 문서가 일부 수치와 모순됩니다. |
| 2 | 영향도 | 🟢 양호 | 기본값이 `txt`라 기존 흐름은 유지됩니다. `sys.path` 전역 변경은 주의가 필요합니다. |
| 3 | SOLID (SRP) | 🟢 양호 | AI Hub 변환 책임이 별도 어댑터로 잘 분리됐습니다. |
| 4 | 선제적 위험 | 🟠 미흡 | 중복 ID, 문자열 ID, 잘못된 `Bbox` 타입을 검증하지 않습니다. |
| 5 | 방어적 입력 처리 | 🟠 미흡 | 필수 키 누락은 실패하지만 빈 값과 구조 오류 일부는 조용히 통과합니다. |
| 6 | 보안 | 🟢 양호 | 로컬 파일만 읽으며 새로운 경로 조작이나 외부 전송은 없습니다. |
| 7 | Silent Failure | 🟠 미흡 | 빈 `Bbox`, 중복 ID, 공백뿐인 `data`가 잘못된 정답을 만들 수 있습니다. |
| 8 | 테스트 | 🟡 보통 | 기본 동작은 검증하지만 이상 데이터와 실제 CLI 연결 테스트가 부족합니다. |
| 9 | 코드 스타일 | 🟡 보통 | 구현은 읽기 쉽지만 신규 테스트에 탭 들여쓰기가 사용됐습니다. |
| 10 | 문서 정합성 | 🔴 미흡 | 처리시간 서술이 표와 다르고, 상대 비교의 유효성을 과하게 단정합니다. |

### Critical 이슈 — Must-fix (머지 차단)

없음.

### High 이슈 — Should-fix (머지 전 수정 권장)

- `docs/bench/2026-07-17-preprocess-ab-2010.md:9,24-27` / deskew 처리시간 설명이 실제 표와 모순됩니다. / 근거(diff 인용): `"처리시간 증가 없음"`이라고 했지만 baseline은 `35.5초`, deskew는 `39.0초`입니다. 약 9.9% 증가입니다. / 제안 수정: `"약 9.9% 증가했으나 절대 증가량은 3.5초"`처럼 사실대로 고치고 판정에도 반영하세요.

- `docs/bench/2026-07-17-preprocess-ab-2010.md:33-34` / 라벨 오류가 상대 비교에 미치는 영향을 지나치게 작다고 단정했습니다. / 근거(diff 인용): 라벨에 실제 토큰이 누락됐다고 밝힌 뒤 `"4개 런에 동일하게 작용하므로 레버 간 우열 판정에는 영향이 작다"`고 결론 냅니다. 하지만 더 좋은 전처리가 누락된 토큰까지 정확히 읽으면 오히려 CER이 악화될 수 있습니다. / 제안 수정: 결과를 “잠정 관찰”로 낮추고, 결과가 달라진 페이지의 원본 대조와 페이지별 paired 비교를 추가하세요. 특히 0.25~0.31%p 차이는 현재 근거만으로 채택·기각을 확정하기 어렵습니다.

- `img2txt/bench/aihub.py:26-28` / ID와 단어 데이터의 형식을 검증하지 않아 잘못된 정답이 조용히 생성될 수 있습니다. / 근거(diff 인용): `sorted(payload["Bbox"], key=lambda entry: entry["id"])`는 중복 ID를 허용하고, 문자열 ID라면 `"10"`이 `"2"`보다 먼저 정렬될 수 있습니다. `" ".join(entry["data"] ...)`도 빈 문자열이나 공백 데이터를 허용합니다. / 제안 수정: `Bbox`가 목록인지, `id`가 정수이며 고유한지, `data`가 비어 있지 않은 문자열인지 검사하고 이상하면 파일 경로가 포함된 `ValueError`를 발생시키세요.

- `docs/bench/2026-07-17-preprocess-ab-2010.md:15-18` / 재현 메타데이터가 축약돼 정확한 결과 추적이 어렵습니다. / 근거(diff 인용): `dataset_hash 7332652ce152...`, contrast/upscale/deskew run ID가 `...15:08:47-...` 형식입니다. JSONL이 커밋되지 않으므로 이 문서가 사실상 유일한 기록입니다. / 제안 수정: 전체 dataset hash와 네 개 run ID를 모두 기록하세요.

### Medium 이슈 (다음 iteration)

- `scripts/bench_ocr.py:16-20` / 모듈을 import하기만 해도 `sys.path`의 최우선 경로가 변경됩니다. / 근거(diff 인용): `sys.path.insert(0, ...)`가 조건 없이 실행됩니다. 테스트나 다른 도구에서 같은 이름의 모듈을 잘못 불러올 수 있습니다. / 제안 수정: 직접 실행 상황에서만 경로를 보정하거나, 가능하면 `python -m` 실행 방식으로 통일하세요.

- `img2txt/bench/aihub.py:26` / 대용량 라벨에서 JSON 원문 문자열과 파싱 객체가 동시에 메모리에 존재합니다. / 근거(diff 인용): `read_text()` 후 `json.loads()`를 사용합니다. / 제안 수정: `label_path.open()`과 `json.load()`를 사용해 원문 전체 문자열의 추가 복사를 줄이세요.

- `tests/bench/test_aihub.py:13-47` / 중복 ID, 문자열 ID, 잘못된 `Bbox` 타입을 검증하는 테스트가 없습니다. / 근거(diff 인용): 현재 테스트는 정상 정렬, 빈 목록, `Bbox` 키 누락만 다룹니다. / 제안 수정: 치명적인 이상 구조를 하나의 매개변수화 테스트로 묶어 추가하세요.

- `tests/bench/test_bench_cli.py:232-243` / CLI 테스트가 인자 파싱만 확인합니다. / 근거(diff 인용): `args.label_format == "aihub"`만 검증하며 실제로 AI Hub 어댑터가 선택되는지는 확인하지 않습니다. / 제안 수정: 작은 JSON 라벨 하나로 `main()` 또는 데이터 로드까지 연결되는 핵심 경로를 한 번 검증하세요.

- `docs/bench/2026-07-17-preprocess-ab-2010.md:20-29` / 플랜에서 요구한 lenient CER이 비교표에 없습니다. / 근거(diff 인용): 표에는 `CER (strict)`와 WER만 있습니다. / 제안 수정: lenient CER을 추가하거나 제외 이유를 적으세요.

- `tests/bench/test_aihub.py:13-47` / 탭 들여쓰기가 프로젝트의 일반적인 4칸 공백 스타일과 다릅니다. / 제안 수정: 공백 4칸으로 통일하세요.

### 긍정적 관찰

- `aihub_label_adapter()`가 별도 모듈로 분리돼 역할이 명확합니다.
- `--label-format` 기본값이 `txt`이고 기존 어댑터를 그대로 사용하므로 하위 호환성이 좋습니다.
- `Bbox`나 `id/data` 키 누락을 삼키지 않고 예외로 드러내는 방향은 적절합니다.
- 동일 표본, 실행 설정, 처리시간과 한계를 문서에 함께 기록한 점은 좋습니다.
- 절대 CER의 라벨 편향과 단일 도메인·50페이지 표본 한계를 숨기지 않고 명시했습니다.
### 종합 평가

- 전체 점수: 5.5/10
- 한줄 총평: 지표 방향은 타당하지만, 빈 정답과 오류 레코드에서 페이지 값과 요약 값이 서로 달라 현재 집계 결과를 신뢰하기 어렵습니다.
- 머지 권고: Must-fix 해결 전 불가

### 관점별 리뷰 (표)

| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | 미흡 | 빈 정답·오류 레코드에서 `PageRecord`와 `summarize()` 결과가 불일치합니다. |
| 2 | 영향도 | 주의 | 기존 `cer_strict`의 의미가 id순 CER에서 읽기순서 CER로 바뀌지만 버전 표시가 없습니다. |
| 3 | SOLID(SRP) | 보통 | 집계에 필요한 원시 count를 저장하지 않아 report가 점수를 다시 계산합니다. |
| 4 | 선제적 위험 | 미흡 | 다열 문서를 행 우선으로 섞는 문제를 진단 플래그도 제대로 찾지 못합니다. |
| 5 | 방어적 입력 | 양호 | 좌표 누락, 타입, NaN·무한대, x/y 길이를 조기에 검사합니다. |
| 6 | 보안 | 양호 | 새 네트워크·명령 실행 경로는 없으며 직접적인 보안 문제는 보이지 않습니다. |
| 7 | Silent Failure | 미흡 | 재측정 시 누락 라벨을 건너뛰고 계속 집계하며, 오류 레코드의 지표도 모순됩니다. |
| 8 | 테스트 충분성 | 미흡 | 다열·퇴화 좌표·NaN·오류 경로·빈 정답 혼합 집계 테스트가 빠졌습니다. 실행 검증은 요청에 따라 하지 않았습니다. |
| 9 | 스타일 | 보통 | 하드코딩 경로, 내부 함수 직접 사용, 중복 JSON 읽기가 남아 있습니다. |
| 10 | 머피의 법칙 | 주의 | 빈 라벨, OCR 오류, 다열 문서가 동시에 나오면 숫자는 생성되지만 의미가 크게 왜곡될 수 있습니다. |

### Critical 이슈 — Must-fix (머지 차단)

1. `scripts/bench_ocr.py:197`, `img2txt/bench/report.py:169` / 오류 레코드의 페이지 지표와 요약 지표가 서로 다릅니다. / 근거: 오류 경로는 `"char_miss_rate=0.0, char_extra_rate=0.0"`으로 저장하지만, 요약은 `"char_multiset_diff(r.normalized_ref, r.normalized_output)"`로 다시 계산합니다. 정답이 있고 출력이 빈 오류 레코드는 JSON에서는 놓침률 0, 요약에서는 놓침률 1이 됩니다. / 제안 수정: OCR 오류를 품질 실패로 셀 것인지 운영 실패로 제외할지 먼저 확정하세요. 품질 실패라면 오류 레코드에도 실제 놓침률을 저장하고, 제외한다면 `error_status` 레코드를 품질 집계에서 빼고 실패율로만 집계해야 합니다.

2. `img2txt/bench/scoring.py:153`, `img2txt/bench/report.py:169` / 빈 정답 출력이 데이터 구성에 따라 추가율에 포함되거나 사라집니다. / 근거: 페이지 함수는 분모가 0이면 `char_extra_rate=0.0`을 반환합니다. 그러나 요약은 `t=0`인 레코드의 `extra`도 분자에 더합니다. 따라서 빈 정답만 있으면 추가율 0이지만, 비어 있지 않은 다른 페이지가 하나라도 있으면 빈 정답의 환각 문자가 그 페이지 분모에 얹혀 추가율을 크게 올립니다. / 제안 수정: 현재 설계처럼 별도 진단으로 처리한다면 `t==0` 레코드는 micro 추가율 분자에서도 제외하고 `empty_ref_extra_chars_total`로 따로 합산하세요. 반대로 포함할 정책이면 페이지 값에 `None` 같은 정의 불가 값을 사용하고 문서와 테스트를 함께 바꿔야 합니다.

### High 이슈 — Should-fix

1. `img2txt/bench/aihub.py:105` / y-밴드 알고리즘은 다열 문서를 행 우선으로 섞습니다. / 근거: 모든 항목을 `y_center`로 묶은 뒤 각 행을 `x_left`로 정렬합니다. 좌측 열을 끝까지 읽은 뒤 우측 열을 읽어야 하는 문서도 `좌1→우1→좌2→우2`가 됩니다. / 제안 수정: 최소한 열 분리 후 열 내부 행 정렬을 적용하거나, 실제 AI Hub 다열·표 샘플을 고정 fixture로 만들어 기대 읽기순서를 검증하세요.

2. `img2txt/bench/aihub.py:146` / `suspicious_layout_flag`가 실제 다열·표 오작동을 탐지하지 못합니다. / 근거: 조건은 `"len(rows) <= 2 and len(bbox) >= 12"`뿐입니다. 정상적인 긴 한 줄은 의심으로 잡지만, 여러 행으로 구성된 2열 문서는 잡지 못합니다. / 제안 수정: x축 간격, 열 군집 수, 행별 x 범위와 불규칙한 y 겹침을 사용하세요. 어렵다면 플래그를 제거하고 아직 탐지하지 못한다고 명시하는 편이 안전합니다.

3. `docs/superpowers/specs/2026-07-13-ocr-llm-quality-harness-design.md:164` / 품질 하한 통과 조건의 방향이 반대로 적혀 있습니다. / 근거: `"놓침률이 ... 통과선 이상일 것"`이라고 했지만 놓침률은 낮을수록 좋습니다. / 제안 수정: “통과선 이하” 또는 “허용 상한 이하”로 고치고 변수명도 `max_miss_rate`처럼 방향이 드러나게 하세요.

4. `img2txt/bench/report.py:34`, `docs/superpowers/specs/2026-07-13-ocr-llm-quality-harness-design.md:130` / 같은 `cer_strict` 필드의 의미가 바뀌지만 지표 버전이 없습니다. / 근거: 문서는 `"cer_strict가 자동으로 읽기순서 CER ... 별도 필드 아님"`이라고 명시합니다. 기존 id순 결과와 신규 결과가 같은 필드명으로 저장됩니다. / 제안 수정: 실행 메타에 `metric_schema_version`과 `reference_order="bbox-yband-v1"`을 넣으세요. 가능하면 `cer_reading_order`를 별도 필드로 두어 과거 결과와의 혼합을 막아야 합니다.

5. `scripts/remeasure_reading_order.py:51` / 재측정이 라벨 누락을 조용히 건너뜁니다. / 근거: `"if not lp.exists(): continue"`라서 일부 페이지만 측정해도 성공 결과처럼 출력됩니다. 또한 검증을 거치지 않고 내부 함수 `_reading_order_words`를 직접 호출합니다. / 제안 수정: 기대 페이지 수와 실제 페이지 수가 다르면 기본적으로 실패시키고, 누락 허용은 명시적 옵션으로 분리하세요. 공개 어댑터 또는 검증된 공통 함수를 사용해야 합니다.

6. `tests/bench/test_aihub.py:100`, `tests/bench/test_report.py:245` / 설계에서 약속한 치명적 실패 테스트가 구현되지 않았습니다. / 근거: 다열·표 테스트는 없고, 진단 테스트도 같은 y의 12개 단어만 확인합니다. `_create_error_records`, NaN·무한대, 0 높이, 빈 정답과 정상 정답이 섞인 micro 집계도 테스트하지 않습니다. / 제안 수정: 핵심 테스트를 최대 3개씩 유지하되 다열 오정렬, 오류 레코드 일관성, 빈 정답 혼합 집계를 우선 추가하세요.

### Medium 이슈

1. `img2txt/bench/dataset.py:65` / AI Hub 라벨을 어댑터와 메타 어댑터가 각각 열어 JSON 파싱과 검증을 두 번 수행합니다. / 제안 수정: 텍스트와 진단 메타를 한 번에 반환하는 내부 파서로 공통화하세요.

2. `scripts/remeasure_reading_order.py:20` / 데이터 경로와 세트가 현재 작업 디렉터리 기준으로 하드코딩되어 다른 위치에서 실행하면 실패합니다. / 제안 수정: 프로젝트 루트 기준 절대 경로를 만들고 CLI 인자로 reports·label 경로를 덮어쓸 수 있게 하세요.

3. `docs/bench/2026-07-17-reading-order-metric.md:4` / 설계 문서 링크가 실제 추가된 경로와 다릅니다. 하단 상대 링크도 같은 디렉터리에 없는 파일을 가리킵니다. / 제안 수정: `../superpowers/specs/2026-07-17-reading-order-metric-design.md`로 바로잡으세요.

4. `docs/review/review-result-agy-20260717-215448.md:1` / 리뷰 결과가 아니라 권한 확인 계획 로그가 들어 있습니다. / 제안 수정: 무효 산출물은 제거하거나 실패 사유 한 줄만 남겨 저장소 잡음을 줄이세요.

5. `docs/bench/2026-07-17-reading-order-metric.md:31` / 읽기순서 CER이 낮아졌다는 사실만으로 “오류 패턴을 더 잘 포착한다”고 결론 내렸습니다. / 제안 수정: 수동 검증 표본이나 정답 읽기순서와의 일치율이 없으므로 “id순 편향이 감소했다” 수준으로 표현을 낮추세요.

### 긍정적 관찰

- 공백 제외 `Counter` 기반 놓침률과 추가율 산식은 순서무관 품질 하한·진단 지표로 타당합니다.
- `empty_ref_with_output` 생산자는 엄격 정규화 후 공백 제거 길이를 사용하고, 정상 채점 레코드의 소비자는 해당 필드를 직접 세므로 기본 경로는 일치합니다.
- `_create_error_records`에 신규 필드 자체가 누락되지는 않았습니다. 다만 값 정책이 요약 로직과 충돌합니다.
- 좌표 누락, 불일치, bool, NaN·무한대를 거부하는 방어 코드는 좋습니다.
- `PagePair.reading_order_meta`의 `default_factory=dict`와 선택적 `meta_adapter`는 기존 호출처 호환성을 유지합니다.
- 기존 CER 구현을 건드리지 않고 정답 생성 경계에서 변경한 점은 변경 범위를 줄이는 좋은 선택입니다.
---

## Claude 크로스 리뷰 (원본 코드/문서 대조)

### 종합 소견
Codex 5.5/10 리뷰의 핵심 지적 다수가 실제 코드/문서 대조에서 real로 확인됨. Must-fix 2건은 동일 근본원인(summarize의 miss/extra micro가 정규화 문자열에서 재계산 → 오류/빈정답 레코드의 저장 필드 0.0과 불일치). 단, 검증된 헤드라인 결과(놓침률 2010 0.98%/b1980 17.8%)는 영향 없음 — 놓침률(주지표)은 빈정답·오류 페이지가 miss=0 기여라 안 흔들리고, 오류 카운트는 전 런 0. 영향은 추가율(진단)의 견고성/일관성에 국한.

### 항목별 판정
| # | Codex 지적 | 파일:라인 | 심각도 | Claude 판정 | 근거(대조) |
|---|-----------|---------|-------|------------|-----------|
| MF1 | 오류 레코드 페이지값 vs 요약 불일치 | bench_ocr.py:192-200, report.py:169 | Critical | 동의(Must-fix) | error 레코드 normalized_ref=실제정답/output="", 저장 char_miss_rate=0.0인데 summarize는 char_multiset_diff 재계산→miss 전체. 확인됨 |
| MF2 | 빈정답 추가 글자가 micro 추가율에 누수 | scoring 산식, report.py:169 | Critical | 동의(Must-fix) | t=0 레코드 extra가 total_extra엔 더해지고 total_ref_ms엔 0. 페이지값 0.0과 불일치. 확인됨 |
| SF3 | 스펙 7.3 놓침률 방향 오기 | design.md:164 | High | 동의(Should) | "통과선 이상일 것" — 놓침률은 낮을수록 좋으므로 "이하"가 맞음. 확인됨 |
| SF4 | cer_strict 의미 변경 버전 표기 없음 | report.py:34 | High | 부분동의(Should) | 사실이나 런은 timestamp/label로 구분됨. metric_schema_version 추가는 유효 개선 |
| SF1 | 다열 y-밴드 행 우선 섞임 | aihub.py | High | 부분동의(기지 한계) | 설계 §10에 명시된 알려진 한계. 판정은 델타/놓침률 기반이라 영향 제한. 열검출은 YAGNI |
| SF2 | suspicious_layout_flag 2열 미탐지 | aihub.py:146 | High | 동의(Should) | over-merge만 잡고 다행 2열 못 잡음. 플래그 한계 명시 또는 개선 필요 |
| SF5 | remeasure 라벨 누락 조용히 skip | remeasure:51 | High | 부분동의(Minor) | 분석 스크립트. skip 시 경고/카운트 노출 권장 |
| SF6 | 치명 실패 테스트 누락 | tests | High | 동의(Should) | MF1/MF2를 잡았을 테스트(오류레코드·빈정답혼합·다열·NaN·0높이) 부재 |
| M3 | 벤치 리포트 설계문서 링크 오류 | reading-order-metric.md:4,55 | Medium | 동의 | 링크가 .superpowers/sdd 및 ./(docs/bench) 가리킴 — 실제는 docs/superpowers/specs. 둘 다 깨짐 |
| M4 | agy 리뷰 결과가 무효 로그 | review-result-agy | Medium | 동의 | 실제 agy 리뷰 실패 산출물. 제거/주석 권장 |
| M1,M2,M5 | 중복 JSON 읽기/하드코딩 경로/과장 표현 | 다수 | Medium | 동의(Minor) | 유효한 개선점 |

### 머지 결정: MERGE_BLOCKED (동의한 Must-fix 2건)

근본 수정: summarize의 miss/extra micro를 오류 레코드(운영 실패, 별도 집계)와 빈정답(t=0, empty_ref 진단으로 별도 포착)을 제외하도록 정정 → 페이지값과 집계 일치. remeasure 스크립트도 동일 정정. 추가로 SF3(방향 오기)·M3(링크)·M4(무효 파일) 문서 정정, SF6 테스트 보강.

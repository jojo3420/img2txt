### 종합 평가
- 전체 점수: 6.5/10
- 한줄 총평: 구조와 테스트 방향은 좋지만, 재현 메타의 정확성과 실패 처리에 허점이 있어 실험 결과를 신뢰하기 어렵습니다.
- 머지 권고: 🟡 Should-fix 수정 후 머지

### 관점별 리뷰 (표)

| # | 관점 | 평가 | 핵심 지적 |
|---|------|------|---------|
| 1 | 정합성 | ⚠️ 부분 충족 | 기능은 연결됐지만 저신뢰 deskew 원본 유지 조건이 구현되지 않음 |
| 2 | 영향도 분석 | ⚠️ 주의 | JSONL 첫 줄 스키마 변경으로 기존 소비 코드가 깨질 수 있음 |
| 3 | SOLID (특히 SRP) | ✅ 양호 | 전처리, 실행, 리포트 책임이 대체로 분리됨 |
| 4 | 선제적 위험 식별 | ❌ 보완 필요 | 동시 실행 시 전처리 파일 충돌 가능 |
| 5 | 방어적 입력 처리 | ⚠️ 보완 필요 | 잘못된 JSON, 다중 채널 이미지, 음수 샘플 수 처리가 부족함 |
| 6 | 보안 (OWASP) | ✅ 특이사항 없음 | 로컬 CLI 범위이며 SQL·웹·인증 공격면은 없음 |
| 7 | Silent Failure 방지 | ❌ 보완 필요 | 손상된 JSON을 텍스트로 위장하고 전체 실패 기록도 저장하지 않음 |
| 8 | 테스트 | ⚠️ 핵심 경로 양호 | 정상 경로는 충분하지만 치명적 실패 경로 테스트가 빠짐 |
| 9 | 코드 스타일/가독성 | ⚠️ 보통 | 일부 파일의 탭 들여쓰기와 누락된 타입 표기가 계획과 불일치 |
| 10 | 머피의 법칙 | ❌ 보완 필요 | 같은 크기의 데이터 변경, 동시 실행, 중첩 디렉터리 등이 고려되지 않음 |

### Critical 이슈 — Must-fix (머지 차단)

없음.

### High 이슈 — Should-fix (머지 전 수정 권장)

#### H1. 실행 메타가 실제 실험 데이터를 식별하지 못함

- 파일: `img2txt/bench/report.py:36-69`
- 문제: `dataset_hash`가 이미지의 파일명과 크기만 사용합니다. 라벨 내용은 전혀 포함하지 않고, 같은 크기로 이미지 내용이 바뀌어도 해시가 같습니다. 실제 처리한 `pairs`가 아니라 디렉터리 전체를 기준으로 계산하는 문제도 있습니다.
- 근거(diff 인용): `f"{f.name}:{f.stat().st_size}"`, `build_run_meta(image_dir=args.image_dir, ...)`
- 제안 수정: `--limit` 적용 후 실제 처리한 이미지와 라벨 목록을 기준으로 SHA-256 콘텐츠 해시를 계산하세요. 최소한 처리한 페이지 ID와 이미지·라벨 해시 목록을 메타에 남겨야 합니다.

#### H2. 전처리 설정값이 메타에 없어 이전 결과를 재현할 수 없음

- 파일: `img2txt/bench/report.py:59-69`, `img2txt/bench/preprocess.py:15-22`
- 문제: 메타에는 `"upscale"` 같은 레버 이름만 기록됩니다. 이후 `CONTRAST_FACTOR`나 deskew 범위가 변경되면 같은 메타로 서로 다른 처리가 실행됩니다.
- 근거(diff 인용): `"preprocess": preprocess`만 기록하며 `CONTRAST_FACTOR`, `DESKEW_STEP_DEGREES` 등은 누락됨
- 제안 수정: `preprocess_config`에 레버 이름과 관련 상수값을 모두 기록하세요. Pillow 버전과 코드 버전 또는 Git 커밋도 함께 남기는 편이 안전합니다.

#### H3. 저신뢰 deskew가 원본을 유지한다는 계획이 구현되지 않음

- 파일: `img2txt/bench/preprocess.py:62-84`
- 문제: 계획은 “신뢰 낮음이면 원본 유지”라고 명시하지만 구현은 점수가 조금이라도 높으면 회전합니다. 노이즈나 글자가 적은 이미지가 잘못 회전될 수 있습니다.
- 근거(diff 인용): `if score > best_score:`와 `if angle == 0.0:`만 존재
- 제안 수정: 0도 점수 대비 최소 개선율과 최소 텍스트량 기준을 두세요. 기준 미달이면 0도를 반환하고, 빈 이미지·노이즈·희박한 텍스트 테스트를 추가하세요.

#### H4. 동시 실행 시 전처리 파일이 서로 덮어써짐

- 파일: `scripts/bench_ocr.py:256-261`, `img2txt/bench/preprocess.py:103-108`
- 문제: 작업 경로가 `preprocessed/<레버>/<원본명>`으로 고정됩니다. 같은 출력 디렉터리에서 동일 레버를 동시에 실행하면 OCR이 읽는 도중 다른 실행이 파일을 덮어쓸 수 있습니다.
- 근거(diff 인용): `work_dir = args.output.parent / "preprocessed" / args.preprocess`, `out_path = work_dir / image_path.name`
- 제안 수정: 실행 시작 시 UUID 기반 `run_id`를 만들고 `preprocessed/<run_id>/<레버>/`를 사용하세요. 임시 파일에 저장한 뒤 원자적으로 교체하거나 실행 종료 후 정리하는 방식이 안전합니다.

#### H5. 손상된 JSON 라벨이 텍스트 라벨로 조용히 분류됨

- 파일: `scripts/inspect_labels.py:24-38`
- 문제: `.json` 파싱 실패를 무시하고 `kind="text"`로 반환합니다. 라벨이 손상됐는데도 정상적인 텍스트 형식처럼 보이므로 어댑터를 잘못 설계할 수 있습니다.
- 근거(diff 인용): `except json.JSONDecodeError: pass` 이후 `"kind": "text"`
- 제안 수정: `kind="invalid_json"`과 파싱 오류를 결과에 기록하거나 명확하게 실패시키세요. 손상된 JSON 테스트도 추가해야 합니다.

### Medium 이슈 (다음 iteration)

#### M1. JSONL 출력 형식이 기존 소비 코드와 호환되지 않을 수 있음

- 파일: `img2txt/bench/report.py:73-84`
- 문제: 첫 줄만 메타 스키마이고 나머지는 `PageRecord`라서 하나의 JSONL 안에 서로 다른 구조가 섞입니다.
- 근거(diff 인용): 메타에는 `record_type`이 있지만 페이지 레코드에는 해당 필드가 없음
- 제안 수정: 모든 레코드에 `record_type`과 `format_version`을 넣거나 메타를 별도 파일로 저장하세요.

#### M2. 다중 채널 이미지와 EXIF 회전 정보 처리가 불명확함

- 파일: `img2txt/bench/preprocess.py:78-84, 103-108`
- 문제: 원본 모드에 그대로 `fillcolor=255`를 사용하며 EXIF 방향 보정도 하지 않습니다. RGB·RGBA·CMYK·회전 정보가 있는 JPEG에서 배경색이나 방향이 달라질 수 있습니다.
- 근거(diff 인용): `image.rotate(angle, expand=True, fillcolor=WHITE)`
- 제안 수정: `ImageOps.exif_transpose()`를 먼저 적용하고 이미지 모드에 맞는 흰색 값을 사용하세요. RGB JPEG 테스트를 추가하면 됩니다.

#### M3. 라벨 인스펙션이 최상위 파일만 검사함

- 파일: `scripts/inspect_labels.py:51`
- 문제: `glob("*")`만 사용하므로 라벨이 하위 디렉터리에 있으면 파일이 없는 것처럼 보고합니다.
- 근거(diff 인용): `label_dir.glob("*")`
- 제안 수정: 중첩 구조를 지원해야 한다면 `rglob("*")`을 사용하고 결과 이름은 상대 경로로 기록하세요. 평면 디렉터리만 지원한다면 CLI에서 이를 명확히 경고해야 합니다.

#### M4. 실행 ID가 충돌하고 시간대가 불명확함

- 파일: `img2txt/bench/report.py:45, 60-69`
- 문제: 초 단위 로컬 시각만 사용하므로 같은 초에 시작한 실행의 `run_id`가 같을 수 있고 시간대도 알 수 없습니다.
- 근거(diff 인용): `datetime.now().isoformat(timespec="seconds")`
- 제안 수정: UTC 시간대와 마이크로초를 사용하고 UUID를 결합하세요.

#### M5. 코드 스타일이 구현 계획과 일부 불일치함

- 파일: `scripts/inspect_labels.py:21-78`, `tests/bench/test_inspect_labels.py:10-47`, `scripts/bench_ocr.py:90-104`
- 문제: 새 인스펙션 코드가 탭 들여쓰기를 사용하고 `_make_recognize_fn`, `_score_page`의 일부 타입 표기가 빠졌습니다.
- 근거(diff 인용): 계획의 “Type Hints 100%” 및 기존 4칸 공백 스타일과 불일치
- 제안 수정: 4칸 공백으로 통일하고 `RecognizeFn`, `PreprocessFn`, `PagePair` 등의 타입을 명시하세요.

### 긍정적 관찰

- `run_points`의 `preprocess_fn` 주입은 OCR 의존성을 잘 격리했고 기존 호출도 깨지지 않는 선택적 인자입니다.
- contrast, upscale, deskew가 독립 레버로 분리되어 A/B 실험 목적과 잘 맞습니다.
- 미등록 레버는 `ValueError`, confidence 범위 오류는 종료 코드 1로 명확히 처리합니다.
- deskew 각도 0에서 `image.copy()`를 반환해 파일 종료 후 이미지 객체가 무효화되는 문제를 피했습니다.
- CLI부터 러너까지 전처리 경로와 confidence 필터를 확인하는 통합 테스트가 포함됐습니다.
- 외부 의존성을 추가하지 않았고 SQL, XSS, 인증·시크릿 노출과 관련된 직접적인 보안 문제는 보이지 않습니다.
---

## Claude 크로스 리뷰 + 머지 결과 (2026-07-16 22시)

- Codex 6.5/10 조건부, Must-fix 0건. 환각 검증: 탭 들여쓰기 주장 1건 기각(grep 0건), 나머지 파일:라인 실코드 확인
- Should-fix 동의 6건 반영 (fix 커밋 eebe9e2): H2 전처리 상수 메타(LEVER_CONFIGS/preprocess_config), H3 deskew 저신뢰 원본 유지(DESKEW_MIN_IMPROVEMENT 1.05), H1 dataset_hash 처리대상 pairs 기준, H4+M4 전처리 경로 run별 격리+uuid run_id, H5 손상 JSON invalid_json 분류, M5 타입 힌트 보강
- follow-up 이월: M1 record_type 전 레코드, M2 EXIF/CMYK, M3 rglob (실측 시 확인)
- 머지: ✅ squash 501cc33 (--delete-branch). 머지 후 main pytest 196 passed 1 skipped

# 세션 핸드오프 — 2026-07-18 07:41 KST

> **모드**: simple-change
> **Feature**: LLM 보정 비교 트랙 착수 준비 (스펙 7절)
> **이전 세션 종결 사유**: 작업 완료(읽기순서 지표 교체 PR #14 머지, 이슈 #11 종료) → 다음 트랙으로 baton

## 한 줄 요약

OCR 채점 지표를 어순 착시에 안 흔들리는 놓침률 병행 지표로 교체 완료(PR #14 머지, #11 종료). 다음 세션은 LLM 보정 비교 실험의 하드 게이트인 이슈 #12(AI Hub 데이터 외부 LLM 전송 컴플라이언스 확인)부터 처리한다.

---

## 다음 세션이 가장 먼저 할 일

🔴 **반드시 (must)** — 컨텍스트 복원 순서:

1. **이 핸드오프 파일을 읽는다** (`docs/handoff/2026-07-18-0741-next-llm-track.md`)
2. **`docs/progress.md` 최상단 블록**(2026-07-18 읽기순서 지표 교체)을 읽는다 — 직전 세션 결과 전체
3. **이슈 #12 본문**을 읽는다: `gh issue view 12 -R jojo3420/img2txt`
4. **스펙 7절**(LLM 보정 가성비 비교 설계)을 읽는다: `docs/superpowers/specs/2026-07-13-ocr-llm-quality-harness-design.md` 7절
5. **아래 "다음 단계 → MUST"부터 시작** — #12 컴플라이언스 확인

---

## 작업 컨텍스트

### 사용자 원본 요청 (이번 세션)

> 이슈 #11(라벨 CER 지표 어순 문제 → 읽기순서 인식 지표로 교체)을 해결하고, 브레인스토밍 → 설계 → 구현 → PR → 리뷰까지 진행.

### 직전 세션 결과 (완료)

- 순서무관 놓침률(품질 하한) + 읽기순서 CER(부작용 감시) + 추가율(진단) 병행 지표 도입. AI Hub 라벨을 좌표 읽기순서로 복원.
- 재측정으로 진짜 OCR 놓침 드러남: 2010 CER 17.4%→놓침 0.98%, b1980 53.2%→17.8%, 전 4세트 읽기순서 CER < id순 CER.
- PR #14 squash 머지(`75688d9`), 이슈 #11 CLOSED, 전체 91 테스트 통과.

### 사용자 제약-금지사항 (반드시 준수)

🔴 **반드시 (must)**:
- 커밋/푸시/병합은 사용자가 요청할 때만 (전역 규칙). progress 스킬만 예외(progress 문서 한정).
- PR base는 `main` (이 repo 기본 브랜치는 docs/web-service-design이지만 OCR 트랙은 main으로 감. 선행 #8/#9/#10 동일).
- 스코프 밖 파일 커밋 금지: `.idea/`, `tobyteam/`, `bench_data/`(gitignore), `package-lock.json`.
- 구현은 서브에이전트 위임(오케스트레이션 게이트: 메인 코드 수정 턴당 2개). 문서(.md)는 게이트 예외.
- AI Hub 데이터를 외부 LLM에 보내는 실측은 #12 확인 전까지 금지(컴플라이언스).

---

## 코드 변경 상태 (git)

- 브랜치: `main` (origin/main 최신, `aa57a3f`). 작업 트리 clean.
- Uncommitted: 없음 (코드 전부 머지됨). 단, 미추적 리뷰 산출물 `docs/review/pr-review-14-20260718-004413.md`(Codex PR 리뷰)와 이전 세션 잔재(`docs/review/pr-review-9-agy-...`, `docs/handoff/2026-07-12-0028-...`)가 워킹트리에 있음 — 이번 트랙과 무관, 필요 시 정리.

### 이번 세션 주요 커밋 (전부 머지됨)

| SHA | 내용 |
|-----|------|
| `75688d9` | feat: 읽기순서 인식 채점 지표로 교체 (#11) (#14) — squash 머지 |
| `aa57a3f` | docs: progress 일지 갱신 |

---

## 결정 사항 (Decisions) — 뒤집지 말 것

| # | 결정 | 근거 |
|---|------|------|
| 1 | 두 지표 병행: 놓침률=절대 품질 하한 게이트, 읽기순서 CER 델타=순서/문장성 부작용 감시(랭킹 축 아님) | 재정렬 CER도 놓침률도 단독으론 불완전(실측 검증). 랭킹은 기존 7.3 비용→속도 유지 |
| 2 | summarize 품질 micro에서 오류 레코드(운영 실패)+빈정답(t=0) 제외 | 페이지별 저장값과 요약 집계 일치(Codex PR 리뷰 Must-fix 2건). 헤드라인 수치 무손상 |
| 3 | 정답 생성 어댑터 한 곳만 읽기순서로 교체, CER 계산 코드 불변 | 변경 범위 최소화 |
| 4 | 놓침률 품질 하한 통과선은 미확정 — LLM 보정 데이터 보고 보정 트랙에서 확정 | baseline 분포 근거 필요 |

---

## 블로커 - 미해결 이슈 (Blockers)

| # | 이슈 | 영향 | 다음 시도 |
|---|------|------|----------|
| 1 | 🔴 이슈 #12: AI Hub 데이터 외부 LLM 전송 허용 여부 미확인 | LLM 실측 트랙 전체 차단(하드 게이트) | AI Hub 71299 이용약관 국외 반출/외부 전송 조항 확인 → 허용 시 진행, 불가 시 대안(별도 정답셋 or 로컬 ollama만) 결정 → 스펙 7절 플랜에 반영. **사용자 판단 필요** |

---

## 다음 단계 (Next)

🔴 **MUST** (LLM 트랙 차단 해소):
- [ ] 이슈 #12: AI Hub 71299 약관의 외부 전송/국외 반출 조항 확인 (사용자 판단)
- [ ] 확인 결과를 스펙 7절 플랜에 반영 (허용/불가에 따라 실험 설계 분기)

🟡 **SHOULD** (트랙 진행):
- [ ] 스펙 7절 LLM 보정 가성비 비교 플랜 작성(writing-plans) 후 실행 — 지표 교체는 이미 완료되어 유의미 측정 가능
- [ ] 7.1 리팩터: `img2txt/backends/cli.py:58` 모델명 하드코딩(`gpt-5.5`) 제거 + `correct_batch`의 model 파라미터 실호출 연결

🟢 **NICE-TO-DO** (여유 시):
- [ ] 비긴급 개선(이번 리뷰 이월): suspicious_layout_flag 다열 탐지 강화, metric_schema_version 도입
- [ ] JPEG 재압축 교란 수정(#13 SHOULD): `img2txt/bench/preprocess.py` deskew 각도 0에서 재저장 안 하도록
- [ ] 미추적 리뷰/핸드오프 산출물 정리

---

## 핵심 파일 경로 (Refs)

| 카테고리 | 경로 |
|---------|------|
| 직전 세션 결과 | `docs/progress.md` 최상단 블록 |
| 상위 스펙(7절 = 다음 트랙) | `docs/superpowers/specs/2026-07-13-ocr-llm-quality-harness-design.md` |
| 지표 설계 스펙 | `docs/superpowers/specs/2026-07-17-reading-order-metric-design.md` |
| 지표 구현 플랜 | `docs/superpowers/plans/2026-07-17-reading-order-metric.md` |
| 재측정 리포트 | `docs/bench/2026-07-17-reading-order-metric.md` |
| PR #14 Codex 리뷰 | `docs/review/pr-review-14-20260718-004413.md` (미커밋) |
| LLM 리팩터 대상 | `img2txt/backends/cli.py:58` |
| 핸드오프 (이 파일) | `docs/handoff/2026-07-18-0741-next-llm-track.md` |

---

## 검증 상태

| 항목 | 상태 | 비고 |
|------|------|------|
| 벤치 테스트 | ✅ 91 passed | 머지 직전 main |
| PR #14 | ✅ MERGED (squash → main) | `75688d9` |
| 이슈 #11 | ✅ CLOSED | 수동 종료(main 머지라 자동종료 안 됨) |
| 이슈 #12 | ⚠️ OPEN, 미확인 | LLM 트랙 하드 게이트 |

---

## 재개 프롬프트 (다음 세션에 그대로 복사-붙여넣기)

```
이전 세션의 작업을 이어받습니다. 다음 핸드오프 파일을 먼저 읽고 컨텍스트를 복원해주세요:

/Users/joel.silver/Workspace/gitroom/python/img2txt/docs/handoff/2026-07-18-0741-next-llm-track.md

복원 순서:
1. 위 핸드오프 파일 전체를 읽는다
2. docs/progress.md 최상단 블록(2026-07-18)과 스펙 7절을 읽는다
3. `gh issue view 12 -R jojo3420/img2txt`로 #12 본문 확인
4. `git status` `git log --oneline -6`로 현재 상태 확인
5. 핸드오프의 "다음 단계 → MUST"(이슈 #12 컴플라이언스 확인)부터 시작
6. 사용자 제약-금지사항(커밋은 요청 시, 외부 전송은 #12 확인 전 금지, PR base=main)을 반드시 준수
7. 결정 사항 표는 뒤집지 않음(변경 시 사용자 확인)

진행 전에 핸드오프 파일을 읽었음을 확인하고, MUST 항목(#12 확인) 착수 방법을 한 줄로 보고해주세요.
```

---

✅ 핸드오프 메모 작성 완료. 다음 세션은 위 재개 프롬프트로 시작.

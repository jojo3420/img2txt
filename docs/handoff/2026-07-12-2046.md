# 세션 핸드오프 — 2026-07-12 20:46 KST

> 모드: simple-change
> Feature: 공개 수익형 서비스 전환 (roadmap 단계)
> 이전 세션 종결 사유: 사용자 인계 (/handoff)

## 한 줄 요약

이미지 포맷 확장(PNG/WEBP/TIFF)을 구현/커밋했고, 개인 도구를 광고+구독 수익형 공개 서비스로 전환하는 로드맵을 작성/리뷰/커밋했다. 다음은 로드맵의 최우선 항목인 6.0 0단계 검증 게이트(단위 경제성 + OCR 품질/비용 스파이크)를 브레인스토밍하는 것이다.

---

## 다음 세션이 가장 먼저 할 일

🔴 반드시 (must) — 컨텍스트 복원 순서:

1. 이 핸드오프 파일을 읽는다 (`docs/handoff/2026-07-12-2046.md`)
2. 로드맵 스펙을 읽는다: `docs/superpowers/specs/2026-07-12-public-service-roadmap-design.md` (특히 5.1, 6.0, 8)
3. 리뷰 리포트를 읽는다: `docs/review/review-result-20260712-203603.md` (Codex + 교차 검증)
4. `git status && git log --oneline -5`로 현재 상태 확인
5. 아래 "다음 단계 MUST"부터 시작: 6.0 0단계 검증 게이트 브레인스토밍

---

## 작업 컨텍스트

### 사용자 원본 요청 (세션 흐름)

1. python3 가상환경 + PyCharm 세팅 (IntelliJ에서 서버 실행 성공까지)
2. jpg/jpeg에서 png 등 포맷 확장 검토 -> 구현
3. 비로그인 상태에서 내 결과 페이지 조회 기능 브레인스토밍
4. 개인 도구를 외부 공개 수익형 서비스로 전환 브레인스토밍 (로그인, 구독, 문구 중립화, LLM mini 모델, OCR 품질, 다국어)
5. 로드맵 자체 적대적 리뷰 + /request-plan-codex 교차 리뷰 후 자동반영
6. /commit push
7. /handoff (이 문서)

### 사용자가 지정한 다음 단계 (원본 인용)

> (2) 이후 최우선인 6.0 0단계 검증 게이트(단위 경제성 + OCR 품질/비용 스파이크)부터 착수하는 걸 권합니다. 브레인 스토밍 진행하기

### 사용자 제약 - 금지사항 (반드시 준수)

🔴 반드시 (must):
- 커밋/푸시는 사용자가 명시 요청할 때만 (임의 커밋 금지).
- 커밋은 feature 단위로 분리, `git add .`/`-A`/`-u` 금지 (화이트리스트 스테이징).
- 스코프 밖 파일 커밋 금지: `.idea/`, `tobyteam/`, `docs/prototype/img2txt-web/package-lock.json`, 기존 `docs/handoff/2026-07-12-0028-*` 는 이번 작업과 무관해 제외 중.
- 오케스트레이션: 실제 구현은 서브에이전트에 위임(메인은 판단/계획/종합). 메인 코드 파일 직접 수정은 턴당 2개 제한.
- 문서/설명은 한국어(기술 용어 영어 병기).

---

## 결정 사항 (Decisions) — 뒤집지 말 것

| # | 결정 | 근거 | 누가 |
|---|------|------|------|
| 1 | 입력 포맷: JPEG + PNG + WEBP + TIFF, HEIC 제외 | WEBP/TIFF는 Pillow 내장(무의존), HEIC는 pillow-heif 의존 필요(공수) | 사용자 |
| 2 | 비로그인 "내 결과": localStorage 목록 + 백엔드 디스크 복구(job.json 스냅샷) 방식 | 로그인 없이 식별, DB 없이 영속. 단 이 기능은 파킹됨(아래) | 사용자 |
| 3 | 서비스 전환은 단일 스펙 불가 -> 서브프로젝트 A~H로 분해 | 다중 서브시스템 | 합의 |
| 4 | 구축 경로는 6.2 MVP-우선 권장(무료 OCR+광고 먼저 공개 후 수익화) | 결제/인증을 수요 검증 후 구축, 리스크 최소화 | 합의(미확정) |
| 5 | 어느 경로든 6.0 0단계 검증 게이트가 최우선 | 인프라 먼저 짓고 사업성 없음 판명 방지 (Codex 제안) | 합의 |
| 6 | LLM 보정 모델은 "고품질 대 저비용"으로 표기, 후보는 사용자 지정 GPT-5.5 대 mini | 모델명 고정은 낡음, 단 사용자 지정 유지 | 합의 |

## 파킹된 항목

- "비로그인 내 결과"(결정 #2) 설계는 서비스 전환으로 상위 맥락이 바뀌어 파킹. 로드맵 서브프로젝트 C(스토리지)와 D(로그인, 소유권 병합 토큰)에서 통합 재개.

---

## 블로커 - 미해결 이슈 (Blockers)

| # | 이슈 | 영향 | 상태 |
|---|------|------|------|
| 1 | OCR 엔진이 macOS 전용(Apple Vision via ocrmac, `img2txt/ocr.py`) | 리눅스 공개 서버 불가. 서브프로젝트 A의 핵심 결정 | 미해결(설계 대기) |
| 2 | 서버측 LLM 보정 API가 스텁(`img2txt/backends/api.py`는 원문 반환) | 다중 사용자 공개 서비스 보정 불가. 서브프로젝트 B | 미해결(설계 대기) |
| 3 | ⚠️ `server/jobs.py` 내부 저장명 `.jpg` 하드코딩(포맷 확장 시 미반영) | 기능 무해(PIL이 내용으로 열기). 정확성 후속 확인 | 미해결(경미) |
| 4 | ⚠️ PyCharm 인터프리터 라벨 "3.10"인데 실제 `.venv`는 pyenv 3.13.0 | 동작 무해(경로 유효), 라벨만 낡음 | 미해결(경미) |

---

## 다음 단계 (Next)

🔴 MUST:
- [ ] 6.0 0단계 검증 게이트 브레인스토밍: (a) 단위 경제성 - 페이지당 원가(클라우드 OCR + LLM API + 스토리지/트래픽) 대 광고 RPM(노출률/차단율/국가단가 반영) 계산 모델과 통과/중단 수치 기준, (b) OCR 품질/비용 스파이크 - 대표 데이터셋으로 후보 엔진(Vision 유지/Tesseract/PaddleOCR/클라우드) 언어별(한/영/일)/세로쓰기/저화질 정확도 + 처리시간 + 페이지당 비용 실측 설계. (브레인스토밍 스킬로 진행: 질문 1개씩 -> 접근법 -> 설계 -> 승인 -> 스펙)

🟡 SHOULD:
- [ ] `server/jobs.py` `.jpg` 하드코딩을 원본 확장자 보존으로 정리할지 결정(블로커 #3)
- [ ] PyCharm 실행 구성 `.idea/runConfigurations/server__uvicorn_.xml`만 따로 커밋할지 사용자 확인

🟢 NICE-TO-DO:
- [ ] 데이터셋 기반 OCR 벤치 자동화 스크립트(0단계 재사용)

---

## 코드 변경 상태 (git)

### 이번 세션 커밋 (모두 origin/main 푸시 완료)

| SHA | 메시지 |
|-----|--------|
| `61ccdb0` | feat: 이미지 입력 포맷 PNG/WEBP/TIFF 확장 (10 files, +171 -29) |
| `d4edc04` | docs: 공개 서비스 전환 로드맵 및 Codex 리뷰 추가 (2 files, +201) |

- 베이스: `ff3c2c8`. 현재 HEAD == origin/main == `d4edc04`. tracked working tree clean.

### 커밋 안 된 untracked (스코프 밖, 의도적 제외)

- `.idea/` (PyCharm 설정 — 실행 구성 포함, 커밋 여부 미정)
- `docs/handoff/2026-07-12-0028-review-followups.md` (이전 세션 산출물)
- `docs/prototype/img2txt-web/package-lock.json`
- `tobyteam/`

---

## 검증 상태

| 항목 | 상태 | 출처 |
|------|------|------|
| 백엔드 테스트(pytest) | ✅ 129 passed | 포맷 확장 서브에이전트 보고 (메인 재실행은 안 함) |
| 프런트 빌드(vite) | ✅ built | 동일 서브에이전트 보고 |
| 서버 기동(uvicorn) | ✅ GET / -> HTTP 200 | 메인이 직접 검증(세션 초반) |
| Codex 로드맵 리뷰 | ✅ 7.5/10, 자동반영 완료 | `docs/review/review-result-20260712-203603.md` |
| 0단계 게이트 설계 | ⚠️ 미착수 (다음 세션 MUST) | - |

---

## 핵심 파일 경로 (Refs)

| 카테고리 | 경로 |
|---------|------|
| 로드맵 스펙 | `docs/superpowers/specs/2026-07-12-public-service-roadmap-design.md` |
| 리뷰 리포트 | `docs/review/review-result-20260712-203603.md` |
| OCR 엔진(블로커1) | `img2txt/ocr.py` |
| LLM 보정 백엔드(블로커2) | `img2txt/backends/api.py`, `img2txt/backends/factory.py` |
| 업로드 검증 | `server/routes.py` (매직넘버 `_is_valid_image_magic`) |
| 잡 저장(블로커3) | `server/jobs.py` |
| PyCharm 실행 구성 | `.idea/runConfigurations/server__uvicorn_.xml` |
| 핸드오프(이 파일) | `docs/handoff/2026-07-12-2046.md` |

---

## 재개 프롬프트 (다음 세션에 그대로 복사-붙여넣기)

```
이전 세션의 작업을 이어받습니다. 다음 핸드오프 파일을 먼저 읽고 컨텍스트를 복원해주세요:

/Users/joel.silver/Workspace/gitroom/python/img2txt/docs/handoff/2026-07-12-2046.md

복원 순서:
1. 위 핸드오프 파일 전체를 읽는다
2. 로드맵 스펙과 리뷰 리포트를 읽는다 (경로는 핸드오프의 Refs 표)
3. git status, git log --oneline -5로 현재 상태 확인
4. 핸드오프의 "다음 단계 MUST" = 6.0 0단계 검증 게이트 브레인스토밍부터 시작
5. 사용자 제약-금지사항(커밋 정책, 스코프 밖 파일 제외, 오케스트레이션 위임)을 준수
6. 결정 사항 표는 뒤집지 않음 (변경 시 사용자 확인)

진행 전에 핸드오프를 읽었음을 확인하고, 0단계 게이트 브레인스토밍의 첫 질문 하나를 던져 시작해주세요.
```
